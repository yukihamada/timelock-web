{
  "name": "timelock-web",
  "version": "1.0.0",
  "description": "時間解放暗号 — drand tlock によるトラストレスな time-release 暗号。ブラウザ完結・鍵を預けない。",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "npm run build && cd public && python3 -m http.server 8799",
    "test": "node test/smoke.mjs"
  },
  "keywords": [
    "timelock",
    "time-release",
    "encryption",
    "drand",
    "tlock",
    "age",
    "ibe"
  ],
  "author": "yukihamada",
  "license": "MIT",
  "dependencies": {
    "buffer": "^6.0.3",
    "esbuild": "^0.28.0",
    "tlock-js": "^0.9.0"
  },
  "devDependencies": {
    "playwright": "^1.58.0"
  }
}
